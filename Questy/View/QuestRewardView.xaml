<UserControl x:Class="Questy.View.QuestRewardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodel="clr-namespace:Questy.ViewModel"
             xmlns:converter="clr-namespace:Questy.Converter"
             xmlns:shinedata="clr-namespace:ShineData.Shine.Structure.Quest;assembly=ShineData"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.DataContext>
        <viewmodel:QuestGeneralViewModel />
    </UserControl.DataContext>
    
    <UserControl.Resources>
        <converter:EnumDescriptionConverter x:Key="EnumDescriptionConverter" />

        <ObjectDataProvider x:Key="QuestRewardUse" MethodName="GetValues" ObjectType="{x:Type system:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="shinedata:QuestRewardUse" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>

        <ObjectDataProvider x:Key="QuestRewardType" MethodName="GetValues" ObjectType="{x:Type system:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="shinedata:QuestRewardType" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>

        <ResourceDictionary x:Key="Components">
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/BaseComponents.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <DataGrid ItemsSource="{Binding SelectedQuest.Rewards}"
                      Resources="{StaticResource Components}" AutoGenerateColumns="False" Margin="5">
                <DataGrid.Columns>
                    <DataGridComboBoxColumn Header="Use" SelectedItemBinding="{Binding Use}" Width="100"
                                            ItemsSource="{Binding Source={StaticResource QuestRewardUse}}">
                        <DataGridComboBoxColumn.ElementStyle>
                            <Style TargetType="ComboBox">
                                <Setter Property="ItemTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <TextBlock
                                                Text="{Binding Converter={StaticResource EnumDescriptionConverter}}" />
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGridComboBoxColumn.ElementStyle>
                        <DataGridComboBoxColumn.EditingElementStyle>
                            <Style TargetType="ComboBox">
                                <Setter Property="ItemTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <TextBlock
                                                Text="{Binding Converter={StaticResource EnumDescriptionConverter}}" />
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGridComboBoxColumn.EditingElementStyle>
                    </DataGridComboBoxColumn>
                    <DataGridComboBoxColumn Header="Type" SelectedItemBinding="{Binding Type}" Width="100"
                                            ItemsSource="{Binding Source={StaticResource QuestRewardType}}">
                        <DataGridComboBoxColumn.ElementStyle>
                            <Style TargetType="ComboBox">
                                <Setter Property="ItemTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <TextBlock
                                                Text="{Binding Converter={StaticResource EnumDescriptionConverter}}" />
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGridComboBoxColumn.ElementStyle>
                        <DataGridComboBoxColumn.EditingElementStyle>
                            <Style TargetType="ComboBox">
                                <Setter Property="ItemTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <TextBlock
                                                Text="{Binding Converter={StaticResource EnumDescriptionConverter}}" />
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGridComboBoxColumn.EditingElementStyle>
                    </DataGridComboBoxColumn>
                    <DataGridTextColumn Header="Experience" Binding="{Binding Value.Experience}" Width="100" />
                    <DataGridTextColumn Header="Money" Binding="{Binding Value.Money}" Width="100" />
                    <DataGridTextColumn Header="Item ID" Binding="{Binding Value.ItemId}" Width="100" />
                    <DataGridTextColumn Header="Item Amount" Binding="{Binding Value.ItemLot}" Width="100" />
                    <DataGridTextColumn Header="Abstate ID" Binding="{Binding Value.AbstateId}" Width="100" />
                    <DataGridTextColumn Header="Abstate Strength" Binding="{Binding Value.AbStateStrength}" Width="100" />
                    <DataGridTextColumn Header="Abstate Keep Time" Binding="{Binding Value.AbStateKeepTime}" Width="100" />
                    <DataGridTextColumn Header="Pet ID" Binding="{Binding Value.PetId}" Width="100" />
                    <DataGridTextColumn Header="Mini House ID" Binding="{Binding Value.MiniHouseId}" Width="100" />
                    <DataGridTextColumn Header="Title Type" Binding="{Binding Value.TitleType}" Width="100" />
                    <DataGridTextColumn Header="Title Element" Binding="{Binding Value.TitleElementNo}" Width="100" />
                    <DataGridTextColumn Header="Kill Points" Binding="{Binding Value.KillPoints}" Width="100" />
                </DataGrid.Columns>
            </DataGrid>
    </Grid>
</UserControl>
