<UserControl x:Class="ShineSuite.Module.Abstate.View.AbstateListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ShineSuite.Module.Abstate.View"
             xmlns:viewmodels="clr-namespace:ShineSuite.Module.Abstate.ViewModel"
             xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance viewmodels:AbstateListModel}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Button Margin="5" HorizontalAlignment="Stretch" Command="{Binding CreateAbstateCommand}">
            <ui:IconAndText Content="Add new Abstate" Icon="{x:Static ui:SegoeFluentIcons.Add}"/>
        </Button>
        <ListBox Grid.Row="1" ItemsSource="{Binding AbnormalState.Records}" SelectedItem="{Binding SelectedAbState}" BorderThickness="0" SelectionMode="Single">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Label Content="{Binding InxName}" HorizontalAlignment="Left" VerticalAlignment="Bottom" Padding="2,0,0,0" FontSize="12" FontFamily="Biome" FontWeight="Normal"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
