<UserControl x:Class="ShineSuite.Module.Abstate.View.GeneralView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ShineSuite.Module.Abstate.View"
             xmlns:viewmodels="clr-namespace:ShineSuite.Module.Abstate.ViewModel"
             xmlns:converter="clr-namespace:ShineSuite.Common.Converter;assembly=ShineSuite.Common"
             xmlns:shinedata="clr-namespace:ShineData.Shine.Structure.Enum;assembly=ShineData"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.DataContext>
        <viewmodels:GeneralViewModel />
    </UserControl.DataContext>

    <UserControl.Resources>
        <converter:EnumDescriptionConverter x:Key="EnumDescriptionConverter" />
        
        <ResourceDictionary x:Key="ShineGrid">
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Style/DataGrid.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>

        <ObjectDataProvider x:Key="SubState" MethodName="GetValues" ObjectType="{x:Type system:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="shinedata:SubState" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        
        <ObjectDataProvider x:Key="SubAbstateAction" MethodName="GetValues" ObjectType="{x:Type system:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="shinedata:SubAbstateAction" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="Abstate">
            <StackPanel>
                <TextBox Text="{Binding SelectedAbState.Id}" IsEnabled="False" />
                <TextBox Text="{Binding SelectedAbState.InxName}" />
                <TextBox Text="{Binding SelectedAbState.SubAbstate}" />
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Row="1" Header="Abstate Memory" IsEnabled="{Binding IsAbstateMemoryVisible}">
            <StackPanel>
                <TextBox Text="{Binding SelectedAbstateMemory.AbstateMemoryStruct}" />
                <TextBox Text="{Binding SelectedAbstateMemory.SubAbstateAction}" />
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="Subabstate" IsEnabled="{Binding IsSubAbStateVisible}">
            <DataGrid ItemsSource="{Binding SelectedSubAbState}" AutoGenerateColumns="False" Resources="{StaticResource ShineGrid}">
                <DataGrid.InputBindings>
                    <KeyBinding Key="N" Modifiers="Control" Command="{Binding AddNewSubAbstateCommand}" />
                </DataGrid.InputBindings>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Strength" Binding="{Binding Strength}" Width="100" />

                    <DataGridComboBoxColumn Header="Type" SelectedItemBinding="{Binding Type}" Width="100"
                                            ItemsSource="{Binding Source={StaticResource SubState}}">
                        <DataGridComboBoxColumn.ElementStyle>
                            <Style TargetType="ComboBox">
                                <Setter Property="ItemTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <TextBlock
                                                Text="{Binding Converter={StaticResource EnumDescriptionConverter}}" />
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGridComboBoxColumn.ElementStyle>
                        <DataGridComboBoxColumn.EditingElementStyle>
                            <Style TargetType="ComboBox">
                                <Setter Property="ItemTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <TextBlock
                                                Text="{Binding Converter={StaticResource EnumDescriptionConverter}}" />
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGridComboBoxColumn.EditingElementStyle>
                    </DataGridComboBoxColumn>

                    <DataGridTextColumn Header="Sub Type" Binding="{Binding SubType}" Width="100" />
                    <DataGridTextColumn Header="Keep Time" Binding="{Binding KeepTime}" Width="100" />
                    
                    <DataGridComboBoxColumn Header="Action Index 01" SelectedItemBinding="{Binding ActionIndex01}" Width="100"
                                            ItemsSource="{Binding Source={StaticResource SubAbstateAction}}">
                        <DataGridComboBoxColumn.ElementStyle>
                            <Style TargetType="ComboBox">
                                <Setter Property="ItemTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <TextBlock
                                                Text="{Binding Converter={StaticResource EnumDescriptionConverter}}" />
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGridComboBoxColumn.ElementStyle>
                        <DataGridComboBoxColumn.EditingElementStyle>
                            <Style TargetType="ComboBox">
                                <Setter Property="ItemTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <TextBlock
                                                Text="{Binding Converter={StaticResource EnumDescriptionConverter}}" />
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGridComboBoxColumn.EditingElementStyle>
                    </DataGridComboBoxColumn>
                    
                    <DataGridTextColumn Header="Action Argument 01" Binding="{Binding ActionArg01}" Width="100" />
                    
                    <DataGridComboBoxColumn Header="Action Index 02" SelectedItemBinding="{Binding ActionIndex02}" Width="100"
                                            ItemsSource="{Binding Source={StaticResource SubAbstateAction}}">
                        <DataGridComboBoxColumn.ElementStyle>
                            <Style TargetType="ComboBox">
                                <Setter Property="ItemTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <TextBlock
                                                Text="{Binding Converter={StaticResource EnumDescriptionConverter}}" />
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGridComboBoxColumn.ElementStyle>
                        <DataGridComboBoxColumn.EditingElementStyle>
                            <Style TargetType="ComboBox">
                                <Setter Property="ItemTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <TextBlock
                                                Text="{Binding Converter={StaticResource EnumDescriptionConverter}}" />
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGridComboBoxColumn.EditingElementStyle>
                    </DataGridComboBoxColumn>
                    
                    <DataGridTextColumn Header="Action Argument 02" Binding="{Binding ActionArg02}" Width="100" />
                    
                    <DataGridComboBoxColumn Header="Action Index 03" SelectedItemBinding="{Binding ActionIndex03}" Width="100"
                                            ItemsSource="{Binding Source={StaticResource SubAbstateAction}}">
                        <DataGridComboBoxColumn.ElementStyle>
                            <Style TargetType="ComboBox">
                                <Setter Property="ItemTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <TextBlock
                                                Text="{Binding Converter={StaticResource EnumDescriptionConverter}}" />
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGridComboBoxColumn.ElementStyle>
                        <DataGridComboBoxColumn.EditingElementStyle>
                            <Style TargetType="ComboBox">
                                <Setter Property="ItemTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <TextBlock
                                                Text="{Binding Converter={StaticResource EnumDescriptionConverter}}" />
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGridComboBoxColumn.EditingElementStyle>
                    </DataGridComboBoxColumn>
                    
                    <DataGridTextColumn Header="Action Argument 03" Binding="{Binding ActionArg03}" Width="100" />
                    
                    <DataGridComboBoxColumn Header="Action Index 04" SelectedItemBinding="{Binding ActionIndex04}" Width="100"
                                            ItemsSource="{Binding Source={StaticResource SubAbstateAction}}">
                        <DataGridComboBoxColumn.ElementStyle>
                            <Style TargetType="ComboBox">
                                <Setter Property="ItemTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <TextBlock
                                                Text="{Binding Converter={StaticResource EnumDescriptionConverter}}" />
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGridComboBoxColumn.ElementStyle>
                        <DataGridComboBoxColumn.EditingElementStyle>
                            <Style TargetType="ComboBox">
                                <Setter Property="ItemTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <TextBlock
                                                Text="{Binding Converter={StaticResource EnumDescriptionConverter}}" />
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGridComboBoxColumn.EditingElementStyle>
                    </DataGridComboBoxColumn>
                    
                    <DataGridTextColumn Header="Action Argument 04" Binding="{Binding ActionArg04}" Width="100" />
                </DataGrid.Columns>
            </DataGrid>
            <!--<TabControl ItemsSource="{Binding SelectedSubAbState}" SelectedItem="{Binding SelectedStrength}">
                <TabControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Strength}"/>
                    </DataTemplate>
                </TabControl.ItemTemplate>
                <TabControl.ContentTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <ComboBox ItemsSource="{Binding Source={StaticResource SubState}}" SelectedItem="{Binding Type}" Margin="0,5,0,0">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Path=., Converter={StaticResource EnumDescriptionConverter}}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                            <TextBox Text="{Binding ActionArg01}"/>
                        </StackPanel>
                    </DataTemplate>
                </TabControl.ContentTemplate>
            </TabControl>-->
        </GroupBox>
    </Grid>
</UserControl>