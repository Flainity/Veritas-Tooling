<Window x:Class="ShineSuite.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShineSuite"
        xmlns:viewmodels="clr-namespace:ShineSuite.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <viewmodels:ShineSuiteViewModel/>
    </Window.DataContext>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" Orientation="Horizontal">
            <Rectangle VerticalAlignment="Stretch" Stroke="DarkGray" Width="1" Margin="2"/>
        </StackPanel>
        
        <Label Grid.Row="0" Grid.Column="0" Content="Shine Suite" FontWeight="Bold" FontSize="16"/>
        <StackPanel Grid.Column="0" Grid.Row="1">
            <Button Content="Abstate Module" Height="30" Command="{Binding LoadAbstateModuleCommand}"/>
        </StackPanel>
        
        <Border Grid.Row="0" Grid.RowSpan="2" Grid.Column="2" Margin="5">
            <ContentControl Content="{Binding CurrentModule}"/>
        </Border>
    </Grid>
</Window>
